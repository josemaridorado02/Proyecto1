<?php
/**
 * Configuración de WordPress generada por Ansible
 */

// ** Configuración de MySQL ** //
define( 'DB_NAME', '{{ wordpress_db_name }}' );
define( 'DB_USER', '{{ wordpress_db_user }}' );
define( 'DB_PASSWORD', '{{ wordpress_db_password }}' );
define( 'DB_HOST', '{{ wordpress_db_host }}' );
define( 'DB_CHARSET', 'utf8mb4' );
define( 'DB_COLLATE', '' );

/**
 * Claves únicas de autenticación y salado
 */
define( 'AUTH_KEY',         '{{ wordpress_auth_key }}' );
define( 'SECURE_AUTH_KEY',  '{{ wordpress_secure_auth_key }}' );
define( 'LOGGED_IN_KEY',    '{{ wordpress_logged_in_key }}' );
define( 'NONCE_KEY',        '{{ wordpress_nonce_key }}' );
define( 'AUTH_SALT',        '{{ wordpress_auth_key }}-salt' );
define( 'SECURE_AUTH_SALT', '{{ wordpress_secure_auth_key }}-salt' );
define( 'LOGGED_IN_SALT',   '{{ wordpress_logged_in_key }}-salt' );
define( 'NONCE_SALT',       '{{ wordpress_nonce_key }}-salt' );

/**
 * Prefijo de tablas de WordPress
 */
$table_prefix = 'wp_';

/**
 * Modo debug de WordPress
 */
define( 'WP_DEBUG', false );

/* ¡Eso es todo, deja de editar! Feliz blogging. */

/** Ruta absoluta al directorio de WordPress. */
if ( ! defined( 'ABSPATH' ) ) {
    define( 'ABSPATH', __DIR__ . '/' );
}

/** Configurar variables y archivos de WordPress. */
require_once ABSPATH . 'wp-settings.php';